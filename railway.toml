# railway.toml - minimal configuration for deploying TimttoApp to Railway
#
# This file provides a basic service definition and recommended environment
# variables. DO NOT commit real secrets here. Set sensitive values (DB URI,
# JWT_SECRET, firebase keys, etc.) in the Railway project environment UI.

[project]
name = "timttoapp"

[[services]]
name = "web"
path = "."
# Start the app directly from source — works for local dev and on Railway when
# you prefer to run JS from `src/`. If you build to `dist/` in CI, adjust to
# `npm run start` instead.
start = "node --enable-source-maps src/server.js"
build = "npm ci && npm run build"

# Recommended environment variables (placeholders). Set these in Railway UI.
[env]
# Port the app listens on (Railway will typically override this automatically)
PORT = "3000"
NODE_ENV = "production"

# MongoDB connection strings (support both common names used by projects)
# Set either MONGO_URI or MONGO_URL in Railway variables to your connection string.
MONGO_URI = ""
MONGO_URL = ""

# JWT secret used by the backend. Set a strong secret in Railway env.
JWT_SECRET = ""

# Optional third-party keys/placeholders
FIREBASE_CONFIG = ""
FIREBASE_STORAGE_BUCKET = ""

# Other suggestions: SENTRY_DSN, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, etc.

# Notes:
# - After creating the Railway project, go to the project Settings -> Variables
#   and add values for MONGO_URI (or MONGO_URL), JWT_SECRET and any other secrets used by the app.
# - Railway provides plugin variables when you add a MongoDB plugin; you can map
#   that plugin variable to MONGO_URI or MONGO_URL in the Railway UI.
