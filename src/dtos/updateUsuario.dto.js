import Joi from 'joi';

export const updateUsuarioDto = Joi.object({
    // Usuario DTO removed — kept as permissive stub to avoid import errors
  });
export const updateUsuarioDto = Joi.object().unknown(true);
