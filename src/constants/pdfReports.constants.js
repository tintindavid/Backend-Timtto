'use strict';

export const PDF_FILENAME_TOKENS = ['consecutivo', 'serial', 'inventario', 'item', 'fecha'];

export const PDF_FILENAME_DEFAULT_TOKENS = ['consecutivo', 'fecha', 'item'];

export const PDF_FILENAME_SEPARATOR = '_';

export const PDF_FILENAME_MAX_LENGTH = 120;
